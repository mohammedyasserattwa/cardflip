import "package:flutter/material.dart";

class AddFlashcards extends StatelessWidget {
  const AddFlashcards({super.key});

  @override
  Widget build(BuildContext context) {
    //TODO: Add a form to add flashcards
    return Container();
  }
}
